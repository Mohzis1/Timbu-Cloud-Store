

/*import styles from './order-button.module.css'; // Import the styles

interface ButtonProps {
  label: string; // Prop for button text
  onClick?: () => void; // Optional prop for click handler
  variant?: 'primary' | 'secondary'; // Optional prop for button variant
  icon: string;  // SVG code as a string
}

const Button: React.FC<ButtonProps> = ({ label, icon, onClick, variant = 'primary' }) => {
  return (
   
    <button className={`${styles.button} ${styles[variant]}`} onClick={onClick}>
     {icont={label || 'Icon'} />} {/* Display icon */}
     /* {label}
    </button>
  );
};*/

/*export default Button;*/